/* =============================================================================
 * src/systems/movementSystem.ts
 * -----------------------------------------------------------------------------
 * RESPONSIBILITY
 *   Advances each enemy toward the house with a wandering offset (SRS FR-10).
 *
 * STATUS: documented stub — implemented in a later phase.
 * ============================================================================= */

import type { GameState } from "@/types/game";

export function movementSystem(state: GameState, _dt: number): GameState {
  // TODO: implemented in a later phase.
  return state;
}
