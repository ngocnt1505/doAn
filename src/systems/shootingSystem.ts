/* =============================================================================
 * src/systems/shootingSystem.ts
 * -----------------------------------------------------------------------------
 * RESPONSIBILITY
 *   Turns a clicked target into projectiles for the equipped weapon (SRS FR-15/FR-16).
 *
 * STATUS: documented stub — implemented in a later phase.
 * ============================================================================= */

import type { GameState } from "@/types/game";

export function shootingSystem(state: GameState, _dt: number): GameState {
  // TODO: implemented in a later phase.
  return state;
}
